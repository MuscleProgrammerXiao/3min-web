<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loong.blog.mapper.BlogPostMapper">

    <resultMap id="BaseResultMap" type="com.loong.blog.model.entity.BlogPost">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="excerpt" column="excerpt" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="LONGVARCHAR"/>
        <result property="publishDate" column="publish_date" jdbcType="DATE"/>
        <result property="readTime" column="read_time" jdbcType="VARCHAR"/>
        <result property="categoryId" column="category_id" jdbcType="INTEGER"/>
        <result property="slug" column="slug" jdbcType="VARCHAR"/>
        <result property="author" column="author" jdbcType="VARCHAR"/>
        <result property="coverImage" column="cover_image" jdbcType="VARCHAR"/>
        <result property="published" column="published" jdbcType="BOOLEAN"/>
        <result property="viewCount" column="view_count" jdbcType="INTEGER"/>
        <result property="likeCount" column="like_count" jdbcType="INTEGER"/>
        <result property="commentCount" column="comment_count" jdbcType="INTEGER"/>
        <result property="createdAt" column="created_at" jdbcType="TIMESTAMP"/>
        <result property="updatedAt" column="updated_at" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,title,excerpt,content,publish_date,read_time,
        category_id,slug,author,cover_image,published,
        view_count,like_count,comment_count,created_at,updated_at
    </sql>

    <select id="listByCategory" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from blog_posts
        where category_id = #{categoryId} and published = true
        order by publish_date desc
    </select>
    
    <select id="getBySlug" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from blog_posts
        where slug = #{slug}
        limit 1
    </select>
    
    <update id="incrementViewCount">
        update blog_posts
        set view_count = view_count + 1
        where id = #{id}
    </update>
    
    <update id="incrementLikeCount">
        update blog_posts
        set like_count = like_count + 1
        where id = #{id}
    </update>
    
    <update id="incrementCommentCount">
        update blog_posts
        set comment_count = comment_count + 1
        where id = #{id}
    </update>
</mapper>